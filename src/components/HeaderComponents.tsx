 import style from '../css/style.module.css';
export const HeaderComponents = () => {
  return (
    <header className={`${style.header} `}>
            <ul className={style['header-components']}>

            </ul>
    </header>
  )
}
